services:
  - type: web
    name: atlas-knowledge
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /
    envVars:
      - key: VITE_API_URL
        sync: false
